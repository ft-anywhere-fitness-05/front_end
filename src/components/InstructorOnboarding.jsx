import React from 'react'
import { connect } from 'react-redux';

const InstructorOnboarding = (props) => {
  const { } = props;

  return (null)
}

const mapStateToProps = (state) => {
  return {
    
  }
}

export default connect(mapStateToProps)(InstructorOnboarding)